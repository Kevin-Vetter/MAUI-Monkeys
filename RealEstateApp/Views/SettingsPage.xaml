<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="RealEstateApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fa="clr-namespace:FontAwesome"
    xmlns:vm="clr-namespace:RealEstateApp.ViewModels"
    Title="SettingsPage"
    x:DataType="vm:SettingsPageViewModel">
    <VerticalStackLayout>
        <Label
            HorizontalOptions="Center"
            Text="Type something here to save it in settings"
            VerticalOptions="Center" />
        <Entry Text="{Binding SettingString}" />
        <Button Command="{Binding SaveSettings}" Text="Save Settings" />
        <Label Text="{Binding GUID}" />
    </VerticalStackLayout>
</ContentPage>